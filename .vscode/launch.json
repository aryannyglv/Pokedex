{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Abrir Edge no localhost",
            "url": "http://127.0.0.1:5500/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}